# The Daily Diff — public mirror

Every edition of The Daily Diff, the signed morning brief that lives inside AI coding agents. Pages here are generated only from editions whose Ed25519 signatures verify — drafts and anything tampered never render.

We never ship code. We ship diffs to a known set of config files, signed, previewed locally against your real setup, applied only when you say yes, and reversible with one command.

Public beta soon.
